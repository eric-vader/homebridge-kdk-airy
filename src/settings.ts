/** Platform name users put in config.json (`"platform": "KDKAiry"`). */
export const PLATFORM_NAME = 'KDKAiry';
/** Must match `name` in package.json. */
export const PLUGIN_NAME = 'homebridge-kdk-airy';
